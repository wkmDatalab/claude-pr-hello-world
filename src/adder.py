"""Case 2: an already-correct file.

Nothing here is broken. Its test passes. A good reviewer should confirm this
and recommend "no change needed" instead of inventing a fix.
"""


def add(a: int, b: int) -> int:
    """Return the sum of two integers."""
    return a + b
